services:
  api:
    build: api
    environment:
      - PORT=3000
  ui:
    build: ui

  nginx:
    image: nginx:latest
    ports:
      - "3001:3001"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - api
      - ui